{
  "name": "playground",
  "private": true,
  "workspaces": [
    "focus-guard"
  ],
  "scripts": {
    "dev": "npm run dev -w focus-guard",
    "build": "npm run build -w focus-guard",
    "lint": "npm run lint -w focus-guard"
  }
}
